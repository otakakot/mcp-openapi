{
  "servers": {
    "openapi": {
      "type": "stdio",
      "command": "docker",
      "args": ["run", "-i", "--rm", "-v", "${workspaceFolder}/openapi.yaml:/openapi.yaml", "mcp-openapi"],
    }
  }
}
